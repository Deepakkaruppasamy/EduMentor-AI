// This file is intentionally a no-op.
// The implementation lives in useUndoToast.ts (rewritten to use React.createElement).
// TypeScript resolves .ts before .tsx, so all imports go to useUndoToast.ts.
export {};
